package ca.mcmaster.se2aa4.island.teamXXX;

import org.json.JSONObject;

// The Search interface defines a contract for any class that performs a search operation.
public interface Search {

    // This method is intended to initiate a search operation.
    // It should return a JSONObject that represents the decision or result of the search.
    public JSONObject search();
}
